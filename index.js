const {genString} = require('string-provider');
const {utcDateTimeFormat} = require('moment-datetime-js');

function genString(length) {
    return genString(length)
}

function utcDateTimeFormat() {
    return utcDateTimeFormat();
}

module.exports = {
    genString,
    utcDateTimeFormat
}

